namespace FreeFlow.UI
{
    /// <summary>The gameplay screen. Its content (level text, progress slider, hint button) is
    /// still driven directly by UIController -- this only makes it a page PageManager can
    /// Open/Close like any other.</summary>
    public class GameplayPage : Page
    {
    }
}
